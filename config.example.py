# Omurtag config

templates = [
    "github:EvgeniGenchev/fastapi_frontend_omurtag_template",
]

# Optional: scan transitive dependencies on create (slower)
# transitive_deps = False

# Optional: control what shows in omurtag list
# show_desc = True
# show_stack = True
